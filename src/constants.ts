export enum TYPES {
  OUTPUT = 'output',
  CUSTOM = 'custom',
  SOURCE = 'source',
  TARGET = 'target',
  SMOOTHSTEP = 'smoothstep',
}

export enum NODE_TYPES {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}
